<section class="section"><div class="wrap"><h2 class="t">{{ blog.title }}</h2>{% for article in blog.articles %}<p style="margin-top:14px"><a href="{{ article.url }}">{{ article.title }}</a></p>{% endfor %}</div></section>
